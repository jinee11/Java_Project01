package chapter09.ex01;

public class AcessModifierOfMember {

	public static void main(String[] args) {
		// 캡슐화 : 정보를 보호하기 위해서 클래스 이름(public, default), 생성자 이름(public, default) 앞에 붙는 접근 제어자 4가지
			//필드, 메소드 (4가지 다)
			/*
			 public : 다른 패키지에서 접근 가능
			 protected : 다른 패키지에서 접근 가능 (단 상속관계일때만 적용)
			 default(명시하지않음) : 같은 패키지에서만 접근 가능, <<접근제어자가 없는 경우>>
			 private : 같은 파일에서만 접근 가능
			 */

	}

}
